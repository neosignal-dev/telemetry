groups:
- name: telemetry.rules
  interval: 30s
  rules:
  - alert: ProcessorDown
    expr: up{job="telemetry-processor"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "Processor is down"
      description: "No metrics from telemetry-processor"
